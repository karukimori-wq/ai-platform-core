# AI Platform Core Agent Instructions

Before substantial architecture, API, capability, usage, integration, deployment, or production-readiness work, consult `karukimori-wq/ai-development-intelligence` using its task retrieval protocol, ranking/index, and `memory/ai-platform-core/`.

## Operational learning

For substantial tasks, follow `core/task-run-standard.md` and `core/operational-learning-loop.md`. Use a non-sensitive `taskRunId` when event capture is available. Retrieve a bounded working set, inspect current code/contracts, verify the result, then classify remembered knowledge as used or rejected. Apply `core/failure-outcome-attribution.md` before prevention/recurrence claims.

## Authority

Current AI Platform Core code remains authoritative for implementation; `professional-platform-contracts` remains authoritative for formal platform contracts. Development intelligence is advisory and must be revalidated against current code/contracts.

## After meaningful work

Evaluate extraction and capture only reusable engineering discoveries, failures/root causes, decisions, patterns, contradictions, or stable evidence. Prefer updating existing knowledge over duplicates.

Never store API keys, prompts containing secrets, tokens, credentials, or personal data in development intelligence.
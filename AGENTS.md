# AI Platform Core Agent Instructions

Before substantial architecture, API, capability, usage, integration, deployment, or production-readiness work, consult `karukimori-wq/ai-development-intelligence` using its task retrieval protocol and compact index. Use `memory/ai-platform-core/` for project-specific prior context.

Current AI Platform Core code remains authoritative for implementation; `professional-platform-contracts` remains authoritative for formal platform contracts. Development intelligence is advisory and must be revalidated against current code/contracts.

After meaningful work, capture reusable engineering discoveries, failures/root causes, decisions, patterns, contradictions, and evidence back into the intelligence repository. Never store API keys, prompts containing secrets, tokens, credentials, or personal data there.